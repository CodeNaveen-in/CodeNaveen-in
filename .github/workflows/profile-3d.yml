# name: generate 3d chart for profile contributions

# on:
#   # run automatically every 24 hours
#   schedule:
#     - cron: "0 */24 * * *" 
  
#   # allows to manually run the job at any time
#   workflow_dispatch:
  
#   # run on every push on the main branch
#   # don't forget to change if you're using 'master' branch
#   push:
#     branches:
#     - main

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     name: generate-github-profile-3d-contrib
#     steps:
#       - uses: actions/checkout@v4
#       - uses: yoshi389111/github-profile-3d-contrib@latest
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           USERNAME: ${{ github.repository_owner }}
#           SETTING_JSON: conf/profile-3d-contrib.json
          
#       # push the content of <build_dir> to a branch
#       # the content will be available at https://raw.githubusercontent.com/<github_user>/<repository>/<target_branch>/<file> , or as github page
#       - name: push SVGs to the output-3d branch
#         uses: crazy-max/ghaction-github-pages@v3.1.0
#         with:
#           target_branch: output-3d-contrib
#           build_dir: profile-3d-contrib
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


# Adding new set edited via Co-Pilot
# name: generate 3d chart for profile contributions

# on:
#   schedule:
#     - cron: "0 0 * * *"
#   workflow_dispatch:
#   push:
#     branches:
#       - main

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     name: generate-github-profile-3d-contrib
#     steps:
#       - uses: actions/checkout@v4

#       - name: Generate 3D chart
#         uses: yoshi389111/github-profile-3d-contrib@latest
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           USERNAME: ${{ github.repository_owner }}
#           SETTING_JSON: conf/profile-3d-contrib.json

#       - name: List output files
#         run: ls -R

#       - name: Push SVGs to output-3d-contrib branch
#         uses: crazy-max/ghaction-github-pages@v3.1.0
#         with:
#           target_branch: output-3d-contrib
#           build_dir: profile-3d-contrib
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


#  Adding File copied from user @danielcshn repo

name: GitHub-Profile-3D-Contrib

permissions:
  contents: write

on:
  schedule: # 03:00 JST == 18:00 UTC
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-3d-contrib
    steps:
      - uses: actions/checkout@v3
      - uses: yoshi389111/github-profile-3d-contrib@0.7.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: ${{ github.repository_owner }}
          MAX_REPOS: 300
      - name: Commit & Push
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add -A .
          git commit -m "generated"
          git push
